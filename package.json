{
  "name": "moviemeet",
  "private": true,
  "scripts": {
    "build": "npm --prefix Frontend/movie ci --include=dev && npm --prefix Frontend/movie run build -- --outDir ../../dist --emptyOutDir"
  }
}

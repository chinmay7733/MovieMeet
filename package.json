{
  "name": "moviemeet",
  "private": true,
  "scripts": {
    "build": "node -e \"require('fs').rmSync('dist', { recursive: true, force: true })\" && npm --prefix Backend ci --include=dev && npm --prefix Frontend/movie ci --include=dev && npm --prefix Frontend/movie run build -- --outDir ../../dist --emptyOutDir"
  }
}
